# Repository Instructions

This file is an example project-local `AGENTS.md`.

Project-local instructions override global defaults when they are more specific.

## Project Layout

```text
src/        application source
tests/      automated tests
docs/       project documentation
scripts/    local developer scripts
```

## Architecture

- API/transport layer: `...`
- Application/use cases: `...`
- Domain: `...`
- Infrastructure/adapters: `...`
- Tests: `...`

## Commands

Install:

```bash
<command>
```

Run tests:

```bash
<command>
```

Run lint:

```bash
<command>
```

Run type/static checks:

```bash
<command>
```

Run format check:

```bash
<command>
```

## Project-Specific Rules

- ...

## Validation Requirements

Before reporting done, run:

```bash
<primary validation command>
```

If the command cannot be run, report the exact reason.
